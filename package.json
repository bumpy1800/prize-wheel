{
  "name": "prize-wheel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Exhibition prize wheel — static web game",
  "scripts": {
    "test": "node --test wheel-logic.test.js lib/store.test.js",
    "start": "npx --yes serve ."
  },
  "dependencies": {
    "mongodb": "^6.19.0"
  }
}
